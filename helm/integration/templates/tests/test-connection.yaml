apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "integration.frontendsvelte.fullname" . }}-test-connection"
  labels:
{{ include "integration.frontendsvelte.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "integration.frontendsvelte.fullname" . }}:{{ .Values.frontendsvelte.service.port }}']
  restartPolicy: Never

---

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "integration.backend.fullname" . }}-test-connection"
  labels:
{{ include "integration.backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "integration.backend.fullname" . }}:{{ .Values.backend.service.port }}']
  restartPolicy: Never
