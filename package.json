{
  "name": "futureon-integration-sample",
  "version": "1.0.0",
  "private": true,
  "license": "",
  "description": "Integration sample",
  "author": "",
  "devDependencies": {},
  "dependencies": {},
  "engines": {
    "node": "~16.15.0"
  },
  "scripts": {
    "tilt": "echo \"Starting tilt on $KUBE_CONTEXT, namespace $KUBE_NAMESPACE\" && tilt up --context $KUBE_CONTEXT --namespace $KUBE_NAMESPACE",
    "tilt-down": "echo \"Stopping tilt on $KUBE_CONTEXT, namespace $KUBE_NAMESPACE\" && tilt down --context $KUBE_CONTEXT"
  }
}
